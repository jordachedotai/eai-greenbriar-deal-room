# Design reference

- `Tokens.dc.html`: the type, color, status, and control tokens from the scheduler build. Values are exact. Copy them.
- `Loop.dc.html`: the five-stage loop the left rail is drawn from. Stage names, order, and which are active.
- Deal Room screens: not yet drawn. Build from the PRD and the tokens; screenshots come back as feedback rounds in `docs/V2_FEEDBACK.md` (create on first round).
