# Kickoff prompt for the Claude Code session

Paste this as the first message in a new Claude Code session opened at `~/Documents/Tools/eai-greenbriar-deal-room` after copying `CLAUDE.md`, `docs/`, and `design-reference/` from this vault folder into it.

---

Read CLAUDE.md, then docs/PRD.md, docs/DATA.md, docs/AGENT.md, docs/DEMO_SCRIPT.md, BUILD_PLAN.md, and docs/SOURCES.md, in that order. Do not read anything outside this repo.

Then, before writing code, give me a short plan for Phase 1 only: the files you will create, the three names files you will hand-write, and any question where the docs contradict each other. Keep the plan under 30 lines. Wait for my go.

Rules that matter most: synthetic deal only, no network in mock mode, the agent never writes, every approval audited, one primary button per screen, no em-dashes in UI copy, bump STORE_VERSION with any store shape change.

When Phase 1 is done, run the app, take screenshots of the login, the board view, and the lanes view in state `midstream`, and stop for feedback before Phase 2.
